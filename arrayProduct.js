function productArray (array) {
  // var currentProduct = 1
  // var productBeforeCurrentValue = []

  // for (var i = 0; i < array.length; i++) {
  //   productBeforeCurrentValue[i] = currentProduct
  //   currentProduct *= array[i]
  // }

  // currentProduct = 1

  // for (var j = productBeforeCurrentValue.length - 1; j >= 0; j--) {
  //   productBeforeCurrentValue[j] *= currentProduct
  //   currentProduct *= array[j]
  // }

  // return productBeforeCurrentValue

}

console.log(productArray([1,3,4,5]))
