function longestPalindrome (string) {
  // map all occurences of a char
  // define longestPal to an empty string
  // if char doesn't have a 2nd position don't iterate over it
  // run the palindrome over a slice of the 1st and next char
    // if the slice is a palindrome assign it to the longestPal variable
}

console.log(longestPalindrome("aibohphobia"));//"aibohphobia"
console.log(longestPalindrome("My dad is a racecar athlete"));// "a racecar a"
